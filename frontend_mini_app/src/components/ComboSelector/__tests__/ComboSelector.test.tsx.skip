// SKIPPED - Unit tests are disabled to avoid conflicts with Playwright E2E tests
// To re-enable, remove .skip extension and install @testing-library/react

/**
 * Tests for ComboSelector component
 */

/*
import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import ComboSelector from '../ComboSelector';
import { Combo } from '@/lib/api/types';

const mockCombos: Combo[] = [
  { id: 1, name: 'Combo 1', price: 100, items: [] },
  { id: 2, name: 'Combo 2', price: 200, items: [] },
];

describe('ComboSelector', () => {
  it('renders combos', () => {
    render(<ComboSelector combos={mockCombos} onSelect={() => {}} />);
    expect(screen.getByText('Combo 1')).toBeInTheDocument();
  });
});
*/
